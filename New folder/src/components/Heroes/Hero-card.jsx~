

export default function HeroCard({hero}){
    return (
        <div>
            <h1>{hero.name}</h1>
            <p>{hero.description}</p>
            <img src={hero.image} alt=""/>
        </div>
    )
}